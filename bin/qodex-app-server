#!/usr/bin/env zsh

set -euo pipefail

readonly DEFAULT_CODEX_HOME="${CODEX_APP_SERVER_HOME:-${CODEX_MIN_HOME:-$HOME/.codex-min}}"

export CODEX_HOME="$DEFAULT_CODEX_HOME"
exec codex app-server --listen stdio:// "$@"
